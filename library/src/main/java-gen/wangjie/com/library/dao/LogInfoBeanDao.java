package wangjie.com.library.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import wangjie.com.library.bean.LogInfoBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOG_INFO_BEAN".
*/
public class LogInfoBeanDao extends AbstractDao<LogInfoBean, Long> {

    public static final String TABLENAME = "LOG_INFO_BEAN";

    /**
     * Properties of entity LogInfoBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property LogInfo = new Property(1, String.class, "logInfo", false, "LOG_INFO");
        public final static Property Log = new Property(2, String.class, "log", false, "LOG");
        public final static Property Ll = new Property(3, String.class, "ll", false, "LL");
    }


    public LogInfoBeanDao(DaoConfig config) {
        super(config);
    }
    
    public LogInfoBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOG_INFO_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"LOG_INFO\" TEXT NOT NULL ," + // 1: logInfo
                "\"LOG\" TEXT NOT NULL ," + // 2: log
                "\"LL\" TEXT NOT NULL );"); // 3: ll
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOG_INFO_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LogInfoBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getLogInfo());
        stmt.bindString(3, entity.getLog());
        stmt.bindString(4, entity.getLl());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LogInfoBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getLogInfo());
        stmt.bindString(3, entity.getLog());
        stmt.bindString(4, entity.getLl());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LogInfoBean readEntity(Cursor cursor, int offset) {
        LogInfoBean entity = new LogInfoBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // logInfo
            cursor.getString(offset + 2), // log
            cursor.getString(offset + 3) // ll
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LogInfoBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLogInfo(cursor.getString(offset + 1));
        entity.setLog(cursor.getString(offset + 2));
        entity.setLl(cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LogInfoBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LogInfoBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LogInfoBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
